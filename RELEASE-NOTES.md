DiceMaster's first shareable release includes shared animated dice rooms, eleven finishes, six bundled material textures, per-roller skin synchronization, varied rolling motion, hidden modifiers applied per die, and optional Windows-encrypted credential storage.

Install using the custom repository URL in the README, or extract the complete manual ZIP including Assets. All participants should update for matching visuals. This build targets Dalamud API 15.

Validation: Release build succeeds; twelve relay test groups and geometry/UV/motion checks pass. In-game polish and Windows remembered-credential behavior still need user testing. This is an early release.
