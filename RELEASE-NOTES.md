DiceMaster v0.3.2 fixes names jumping around under At the table during room polling. Participants now remain alphabetically ordered, with a stable participant-ID tie-breaker for duplicate names. GM assignment rows use the same stable ordering.

No relay update is required. Existing skins, remembered authentication and per-die modifiers are unchanged. The Release build passes against Dalamud API 15.
